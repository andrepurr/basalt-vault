[global]
# Halmos symbolic verification config for Basalt Vault

# ── Solver Timeouts ──
solver-timeout-assertion = "2m"
solver-timeout-branching = "10s"

# ── Execution Bounds ──
loop = 8
depth = 0
storage-layout = "solidity"
